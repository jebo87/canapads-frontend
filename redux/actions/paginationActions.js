export const setPageSelected = (selectedPage) => ({
	type: 'SET_SELECTED_PAGE',
	selectedPage
});
export const setPages = (pages) => ({
	type: 'SET_PAGES',

	pages
});
export const setPagesVisible = (pagesVisible) => ({
	type: 'SET_PAGES_VISIBLE',

	pagesVisible
});
