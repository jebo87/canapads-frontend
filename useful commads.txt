we have to launch:

1. Docker for the postgres database for IDSV
2. IDSV
3. go-postgres-monitor
4. ssh to elastic.makakolabs.ca to launch the elastic search
5. makako-api
6. makako-gateway
7. yarn run dev-server react-ads

haproxy -f /usr/local/etc/haproxy.cfg


#polygons for mapbox
1. You can get polygon coordenates in json for using with googlemaps using openstreetmap. Go to http://nominatim.openstreetmap.org/ search a place like "laval"
click on "details"

Look for OSM ID and copy it (control+c), example: 3532125

paste the ID in http://polygons.openstreetmap.fr/index.py and download the polygon or https://global.mapit.mysociety.org/#api-by_area_id

git lg1 > show logs with branches and graphs
git lg2 > show logs with branches and graphs